import { LegalPagePlaceholder } from "@/components/LegalPagePlaceholder";

export const metadata = {
  title: "Termini di Servizio | Alètheia4Job",
  robots: { index: false, follow: false },
};

export default function CompanyTermsPage() {
  return <LegalPagePlaceholder title="Termini di Servizio" />;
}
